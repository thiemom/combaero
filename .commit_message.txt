feat(gui): add discrete_loss element with full heat transfer support

Add PressureLossElement integration as discrete_loss node type:
- Frontend: inspector with correlation selection (constant/linearθ),
  area inheritance from upstream, theta source dropdown
- Frontend: convective surface configuration (Nu/f multipliers)
- Frontend: live telemetry via enriched diagnostics
- Frontend: initial guess overrides (type-aware, m_dot only for elements)
- Frontend: global reset button for all user guesses
- Frontend: improved UX - onClear callback allows clearing inputs to
  auto-infer from topology (e.g., discrete_loss area)

Backend:
- graph_builder: discrete_loss → PressureLossElement with surface
- graph_builder: _expand_initial_guess helper for P/T/m_dot short keys
- schemas: add surface/multipliers to DiscreteLossData, remove stale
  pressure_loss from CombustorData
- export: unit-annotated columns (P [Pa], T [K], etc.), species-named
  compositions (Y[N2], X[O2]), is_boundary flag, combustion_method
- units.py: authoritative unit catalogue

Python API:
- PressureLossElement: convective surface support, htc_and_T, full
  diagnostics (Pi/Po, Ti/To, Mach, Re, transport props, Nu, htc, T_aw)
- CombustorNode: expose m_dot in diagnostics for GUI telemetry

Tests:
- _expand_initial_guess: 8 tests including broadcast precedence fix
- PressureLossElement: convective surface, diagnostics, wall coupling
- CombustorNode: m_dot diagnostic exposure
- Export: units and species-name composition labels

BREAKING CHANGE: CombustorData.pressure_loss field removed; use
separate discrete_loss elements on connecting edges.
