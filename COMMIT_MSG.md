feat: add diagnostics discovery panel and auto-promote scalar results

- Modifies `QUANTITY_CATALOGUE` to present pressures in `bar` instead of `Pa`.
- Implements `discoverFields` to automatically detect parameters.
