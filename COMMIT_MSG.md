docs: modernize and streamline documentation suite

- Audit and prune `docs/` directory; archived stale implementation reports to `docs/archive/`.
- Streamline root `README.md` into a human-centric landing page.
- Extract technical build and development workflows to `docs/BUILDING.md` and `docs/DEVELOPMENT.md`.
- Establish agent-friendly API references in `docs/API_CPP.md` and `docs/API_PYTHON.md` with SI unit annotations.
- Create `docs/GUI_TECHNICAL.md` for JSON schemas and update `gui/README.md`.
- Update `GEMINI.md` and `CLAUDE.md` to enforce documentation hygiene and API sync rules.
- Modernize READMEs in `python/`, `scripts/`, `thermo_data_generator/`, and `gui/frontend/`.
- Finalize `docs/HYGIENE.md` with a detailed audit of remaining technical debt and property gaps.
